extends /_layout.jade

block vars
  - var title = 'Color'

block content
  article
    :md
      # Color

  each family in colorFamilies
    - console.dir(family)
    each variant in family.variants
      .color #{family.familyName}, #{variant.variant}, #{variant.colorName}
